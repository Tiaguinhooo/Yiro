<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet" />
    <title>Product Management</title>
    <style>
      body::-webkit-scrollbar {
        display: none;
      }
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background: white url('/Images/background.png') no-repeat center center fixed;
        background-size: cover;
        color: white;
        min-height: 100vh;
      }

      nav {
        position: sticky;
        top: 0;
        background: #f0f0f0;
        padding: 10px 20px;
        border-bottom: 1px solid #ccc;
        color: #333;
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 1000;
      }
      nav div p {
        margin: 0;
        font-weight: bold;
        font-size: 1.1rem;
      }
      nav a {
        color: #333;
        margin-left: 15px;
        text-decoration: none;
        position: relative;
        font-weight: 600;
        transition: color 0.25s ease;
      }
      nav a:hover {
        color: #000;
      }
      nav a::after {
        content: "";
        position: absolute;
        width: 0;
        height: 2px;
        left: 0;
        bottom: -3px;
        background: currentColor;
        transition: width 0.25s ease;
      }
      nav a:hover::after {
        width: 100%;
      }

      main {
        padding: 20px;
        margin: 20px auto;
        max-width: 1100px;
        background: rgba(0, 0, 0, 0.6);
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
        border-radius: 8px;
        box-sizing: border-box;
        overflow-x: auto; /* horizontal scroll if needed */
      }

      .notification {
        background: #28a745;
        padding: 12px 20px;
        border-radius: 5px;
        margin-bottom: 15px;
        text-align: center;
        font-weight: 600;
        font-size: 1rem;
        animation: fade-in 0.4s ease-in-out;
      }
      @keyframes fade-in {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      form.search-form {
        margin-bottom: 20px;
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        align-items: center;
      }
      form.search-form input[type="text"],
      form.search-form select {
        padding: 10px 14px;
        font-size: 1rem;
        border: none;
        border-radius: 4px;
        background: rgba(255, 255, 255, 0.15);
        color: white;
        transition: background 0.3s ease;
        min-width: 180px;
      }
      form.search-form input[type="text"]::placeholder {
        color: rgba(255, 255, 255, 0.7);
      }
      form.search-form input[type="text"]:focus,
      form.search-form select:focus {
        outline: none;
        background: rgba(255, 255, 255, 0.3);
      }
      form.search-form button {
        padding: 10px 18px;
        font-size: 1rem;
        cursor: pointer;
        background: #555;
        border: none;
        border-radius: 4px;
        color: white;
        font-weight: 600;
        transition: background 0.3s ease;
      }
      form.search-form button:hover {
        background: #333;
      }

      /* TABLE */
      table {
        width: 100%;
        border-collapse: collapse;
        background: rgba(255, 255, 255, 0.15);
        color: white;
        border-radius: 6px;
        overflow: hidden;
        table-layout: fixed;
        box-sizing: border-box;
      }
      thead {
        background: rgba(255, 255, 255, 0.25);
      }
      th, td {
        padding: 12px 16px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        text-align: left;
        font-size: 1rem;
        white-space: nowrap;
      }
      th {
        cursor: pointer;
        user-select: none;
        font-weight: 700;
        position: relative;
      }
      th:hover {
        background: rgba(255, 255, 255, 0.3);
      }
      tr:hover {
        background: rgba(255, 255, 255, 0.1);
      }

      /* Sort icons */
      th::after {
        content: "";
        position: absolute;
        right: 10px;
        top: 50%;
        transform: translateY(-50%);
        font-size: 0.7rem;
      }

      /* Columns widths */
      table th:nth-child(1),  /* Name */
      table td:nth-child(1) {
        width: 220px;
        max-width: 220px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      table th:nth-child(2), /* Price */
      table td:nth-child(2) {
        width: 90px;
        max-width: 90px;
        text-align: right;
        white-space: nowrap;
      }
      table th:nth-child(3), /* Description */
      table td:nth-child(3) {
        max-width: 380px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }
      table th:nth-child(4), /* Category */
      table td:nth-child(4) {
        width: 150px;
        max-width: 150px;
        white-space: nowrap;
      }
      table th:nth-child(5), /* PublisherID */
      table td:nth-child(5) {
        width: 80px;
        max-width: 80px;
        white-space: nowrap;
        text-align: center;
      }

      /* Action buttons inline in same cell */
      td:last-child {
        white-space: nowrap;
      }
      td:last-child form {
        display: inline-block;
        margin: 0 4px 0 0;
      }
      .action-btn {
        padding: 6px 12px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-weight: 600;
        transition: background-color 0.3s ease;
        white-space: nowrap;
      }
      .edit {
        background: #3273a8;
        color: white;
      }
      .edit:hover {
        background: #205b8a;
      }
      .delete {
        background: #a83232;
        color: white;
      }
      .delete:hover {
        background: #6e2020;
      }

      .pagination {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 25px;
        justify-content: center;
      }
      .pagination a {
        padding: 8px 14px;
        border-radius: 4px;
        border: 1px solid white;
        text-decoration: none;
        color: white;
        font-weight: 600;
        transition: background-color 0.3s ease, color 0.3s ease;
      }
      .pagination a:hover {
        background-color: white;
        color: black;
      }
      .pagination .active {
        background-color: white;
        color: black;
        pointer-events: none;
      }
    </style>
  </head>
  <body>
    <nav>
      <div><p>Admin Panel</p></div>
      <div>
        <a href="/admin">Admin Dashboard</a>
        <a href="/admin/users">User Management</a>
        <a href="/admin/products">Product Management</a>
        <a href="/admin/activity-logs">Activity Logs</a>
        <a href="/logout">Logout</a>
      </div>
    </nav>
    <main>
      <% if (success) { %>
        <div class="notification">✅ Successfully edited product!</div>
        <script>
          setTimeout(() => document.querySelector('.notification')?.remove(), 3000);
        </script>
      <% } %>
      <h1>Product Management</h1>
      <form method="GET" action="/admin/products" class="search-form">
        <input type="text" name="search" placeholder="Search by name or description" value="<%= search %>" />
        <select name="category">
          <option value="" <%= (!category || category === '') ? 'selected' : '' %> style="color: black;">All categories</option>
          <% categories.forEach(cat => { %>
            <option value="<%= cat.ID %>" <%= category == cat.ID ? 'selected' : '' %>><%= cat.Name %></option>
          <% }) %>
        </select>
        <button type="submit">Filter</button>
        <input type="hidden" name="sort" value="<%= sort %>" />
        <input type="hidden" name="order" value="<%= order %>" />
      </form>
      <table>
        <thead>
          <tr>
            <% ['Name', 'Price', 'Description', 'CategoryName', 'PublisherID'].forEach(col => {
              let displayCol = col === 'CategoryName' ? 'Category' : col;
              const newOrder = (sort === col && order === 'ASC') ? 'DESC' : 'ASC';
              const arrow = sort === col ? (order === 'ASC' ? '▲' : '▼') : '';
            %>
              <th onclick="window.location='/admin/products?<%= `page=${currentPage}&search=${encodeURIComponent(search)}&category=${encodeURIComponent(category)}&sort=${col}&order=${newOrder}` %>'" style="cursor:pointer; user-select:none;">
                <%= displayCol %> <%= arrow %>
              </th>
            <% }) %>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (!products.length) { %>
            <tr><td colspan="6" style="text-align:center;">No products found.</td></tr>
          <% } %>
          <% products.forEach(product => { %>
            <tr>
              <td title="<%= product.Name %>"><%= product.Name.length > 30 ? product.Name.substring(0, 27) + '...' : product.Name %></td>
              <td>$<%= (typeof product.Price === 'number') ? product.Price.toFixed(2) : 'N/A' %></td>
              <td title="<%= product.Description %>"><%= product.Description.length > 50 ? product.Description.substring(0, 47) + '...' : product.Description %></td>
              <td><%= product.CategoryName || product.CategoryID %></td>
              <td><%= product.PublisherID %></td>
              <td>
                <form action="/admin/products/edit/<%= product.ID %>" method="GET" style="display:inline;">
                  <button class="action-btn edit" type="submit">Edit</button>
                </form>
                <form action="/admin/products/delete/<%= product.ID %>" method="POST" style="display:inline;" onsubmit="return confirm('Are you sure you want to delete this product?');">
                  <button class="action-btn delete" type="submit">Delete</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
      <% if (totalPages > 1) { %>
        <div class="pagination">
          <% for (let i = 1; i <= totalPages; i++) { %>
            <a href="/admin/products?<%= `page=${i}&search=${encodeURIComponent(search)}&category=${encodeURIComponent(category)}&sort=${sort}&order=${order}` %>" class="<%= i === currentPage ? 'active' : '' %>"><%= i %></a>
          <% } %>
        </div>
      <% } %>
    </main>
  </body>
</html>
