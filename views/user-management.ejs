<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet" />
    <title>User Management</title>
    <style>
      /* Reset e base */
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background: white url('/Images/background.png') no-repeat center center fixed;
        background-size: cover;
        color: white;
        min-height: 100vh;
      }

      /* NAVBAR */
      nav {
        position: sticky;
        top: 0;
        background: #f0f0f0;
        padding: 10px 20px;
        border-bottom: 1px solid #ccc;
        color: #333;
        display: flex;
        justify-content: space-between;
        align-items: center;
        z-index: 1000;
      }
      nav div p {
        margin: 0;
        font-weight: bold;
        font-size: 1.1rem;
      }
      nav a {
        color: #333;
        margin-left: 15px;
        text-decoration: none;
        position: relative;
        font-weight: 600;
        transition: color 0.25s ease;
      }
      nav a:hover {
        color: #000;
      }
      nav a::after {
        content: "";
        position: absolute;
        width: 0;
        height: 2px;
        left: 0;
        bottom: -3px;
        background: currentColor;
        transition: width 0.25s ease;
      }
      nav a:hover::after {
        width: 100%;
      }

      /* MAIN CONTAINER */
      main {
        padding: 20px;
        margin: 20px auto;
        max-width: 960px;
        background: rgba(0, 0, 0, 0.6);
        border-radius: 8px;
        box-shadow: 0 0 15px rgba(0, 0, 0, 0.5);
      }

      /* NOTIFICAÇÃO */
      .notification {
        background: #28a745;
        padding: 12px 20px;
        border-radius: 5px;
        margin-bottom: 15px;
        text-align: center;
        font-weight: 600;
        font-size: 1rem;
        animation: fade-in 0.4s ease-in-out;
      }
      @keyframes fade-in {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      /* FORMULÁRIO DE FILTRO E PESQUISA */
      form.search-form {
        margin-bottom: 20px;
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        align-items: center;
      }
      form.search-form input[type="text"],
      form.search-form select {
        padding: 10px 14px;
        font-size: 1rem;
        border: none;
        border-radius: 4px;
        background: rgba(255, 255, 255, 0.15);
        color: white;
        transition: background 0.3s ease;
        min-width: 180px;
      }
      form.search-form input[type="text"]::placeholder {
        color: rgba(255, 255, 255, 0.7);
      }
      form.search-form input[type="text"]:focus,
      form.search-form select:focus {
        outline: none;
        background: rgba(255, 255, 255, 0.3);
      }
      form.search-form button {
        padding: 10px 18px;
        font-size: 1rem;
        cursor: pointer;
        background: #555;
        border: none;
        border-radius: 4px;
        color: white;
        font-weight: 600;
        transition: background 0.3s ease;
      }
      form.search-form button:hover {
        background: #333;
      }

      /* TABELA */
      table {
        width: 100%;
        border-collapse: collapse;
        background: rgba(255, 255, 255, 0.15);
        color: white;
        border-radius: 6px;
        overflow: hidden;
      }
      thead {
        background: rgba(255, 255, 255, 0.25);
      }
      th, td {
        padding: 12px 16px;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        text-align: left;
        font-size: 1rem;
      }
      th {
        cursor: pointer;
        user-select: none;
        font-weight: 700;
        position: relative;
      }
      th:hover {
        background: rgba(255, 255, 255, 0.3);
      }
      tr:hover {
        background: rgba(255, 255, 255, 0.1);
      }

      /* Ícones de ordenação */
      th::after {
        content: "";
        position: absolute;
        right: 10px;
        top: 50%;
        transform: translateY(-50%);
        font-size: 0.7rem;
      }

      /* AÇÕES (EDITAR, DELETAR) */
      .action-btn {
        padding: 6px 12px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-weight: 600;
        margin-right: 8px;
        transition: background-color 0.3s ease;
      }
      .edit {
        background: #3273a8;
        color: white;
      }
      .edit:hover {
        background: #205b8a;
      }
      .delete {
        background: #a83232;
        color: white;
      }
      .delete:hover {
        background: #6e2020;
      }

      /* PAGINAÇÃO */
      .pagination {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
        margin-top: 25px;
        justify-content: center;
      }
      .pagination a {
        padding: 8px 14px;
        border-radius: 4px;
        border: 1px solid white;
        text-decoration: none;
        color: white;
        font-weight: 600;
        transition: background-color 0.3s ease, color 0.3s ease;
      }
      .pagination a:hover {
        background-color: white;
        color: black;
      }
      .pagination .active {
        background-color: white;
        color: black;
        pointer-events: none;
      }
      select {
        background-color: #222; /* fundo escuro */
        color: black;
      }
      select option {
        color: black;
      }
    </style>
  </head>
  <body>
    <nav>
      <div><p>Admin Panel</p></div>
      <div>
        <a href="/admin">Admin Dashboard</a>
        <a href="/admin/users">User Management</a>
        <a href="/admin/activity-logs">Activity Logs</a>
        <a href="/logout">Logout</a>
      </div>
    </nav>
    <main>
      <% if (success) { %>
        <div class="notification">✅ Utilizador editado com sucesso!</div>
        <script>
          setTimeout(() => document.querySelector('.notification').remove(), 3000);
        </script>
      <% } %>

      <h1>User Management</h1>

      <form method="GET" action="/admin/users" class="search-form">
        <input type="text" name="search" placeholder="Search by name or email" value="<%= search %>">
        <select name="role">
          <option value="" <%= (!role || role === '') ? 'selected' : '' %> style="color: black;">All roles</option>
          <option value="admin" <%= role === 'admin' ? 'selected' : '' %>>Admin</option>
          <option value="user" <%= role === 'user' ? 'selected' : '' %>>User</option>
        </select>
        <button type="submit">Filter</button>
        <input type="hidden" name="sort" value="<%= sort %>">
        <input type="hidden" name="order" value="<%= order %>">
      </form>

      <table>
        <thead>
          <tr>
            <% ['Username', 'Email', 'Role', 'Registration Date'].forEach(col => {
                const colKey = col==='Registration Date' ? 'Registration Date' : col;
                const newOrder = (sort===colKey && order==='ASC') ? 'DESC' : 'ASC';
                const arrow = sort===colKey ? (order==='ASC' ? '▲' : '▼') : '';
            %>
              <th onclick="window.location='/admin/users?<%= `search=${search}&role=${role}&sort=${colKey}&order=${newOrder}` %>'">
                <%= col %> <%= arrow %>
              </th>
            <% }) %>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% if (!users.length) { %>
            <tr><td colspan="5" style="text-align:center;">No users found.</td></tr>
          <% } %>
          <% users.forEach(user => { %>
            <tr>
              <td><%= user.Username %></td>
              <td><%= user.Email %></td>
              <td><%= user.Role %></td>
              <td><%= new Date(user['Registration Date']).toLocaleDateString() %></td>
              <td>
                <form action="/admin/users/<%= user.ID %>/edit" method="GET" style="display:inline">
                  <button class="action-btn edit" type="submit">Edit</button>
                </form>
                <form action="/admin/users/<%= user.ID %>?_method=DELETE" method="POST" style="display:inline" onsubmit="return confirm('Delete this user?');">
                  <button class="action-btn delete" type="submit">Delete</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>

      <% if (totalPages > 1) { %>
        <div class="pagination">
          <% for (let i = 1; i <= totalPages; i++) { %>
            <a href="/admin/users?<%= `page=${i}&search=${search}&role=${role}&sort=${sort}&order=${order}` %>"
              class="<%= i===currentPage ? 'active' : '' %>"><%= i %></a>
          <% } %>
        </div>
      <% } %>
    </main>
  </body>
</html>